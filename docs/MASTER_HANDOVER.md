# Master Handover

*Committed canonical state. Curated manually after each milestone. New Claude instances read this first.*

## Current State
- **Branch**: `feat/workout-cool-section-4-checkpoint-3` (origin/main at `7a77315` = §4 checkpoint 2). Checkpoint 4 (mapping curation) work in progress, uncommitted.
- **Last committed milestone on `main`**: workout.cool §4 checkpoint 2 — free-exercise-db assets vendored under `static/vendor/free-exercise-db/` (LICENSE, NOTICE.md, VERSION pin, `exercises.json`, 873 image folders). Shipped via PR #19 (`7a77315`).
- **Last committed milestone on the branch**: workout.cool §4 checkpoint 3 — `scripts/map_free_exercise_db.py` produces `data/free_exercise_db_mapping.csv` (1,897 auto rows) and `docs/workout_cool_integration/checkpoint3_coverage.md` coverage report (`1ff57ff`). No DB writes yet.
- **In-branch (uncommitted) checkpoint 4 state (2026-05-14)**: `scripts/curate_free_exercise_db_mapping.py` (new) flipped 98 rows `auto` → `confirmed` via the structural-equivalence rule, and a follow-up manual pass flipped a further 10 rows to `manual` (starter-plan + common-strength obvious matches) and 5 rows to `rejected` (clear false-positive high-score auto suggestions). Final review-status distribution: 1784 auto, 98 confirmed, 10 manual, 5 rejected — **113 reviewed** rows total, clearing the §4.7 tertiary acceptance bar (≥50 reviewed). Scoped apply dry-run on the 108 confirmed+manual rows passes cleanly; the unscoped apply dry-run still aborts on the pre-existing trailing-whitespace catalogue row at CSV line 909 (`'Dumbbell Shoulder Internal Rotation '`), fix path deferred to checkpoint 5. Also retired a stale `assert rows == []` line in `tests/test_free_exercise_db_mapping.py::TestMappingCsv::test_csv_passes_validator` that pre-dated the checkpoint-3 CSV population.
- **Last verified pytest**: targeted §4 gate **79 passed in 2.52s on 2026-05-14** (`tests/test_free_exercise_db_mapping.py`, after the stale-assertion fix). Adjacent regression run remains 122 passed in 20.09s from 2026-05-11. Last full pytest baseline remains 1160 passed in 159.70s on 2026-05-10.
- **Last verified E2E (Chromium)**: no E2E run for the checkpoint-4 data/test slice (no UI changes). Last verified E2E gate: §5 targeted 52 passed in 1.6m on 2026-05-11 (`e2e/workout-plan.spec.ts`, `e2e/workout-log.spec.ts`). Last full E2E baseline remains 414 passed / 1 failed on 2026-05-10; failure is pre-existing & out-of-scope: `nav-dropdown.spec.ts:117` dark-mode toggle off-viewport at 1440 width.

## Active Workstreams
| Workstream | Status | Next safe step | Docs |
|---|---|---|---|
| Avi Lewis AI workflow refit | complete through Tier 2 + T3.1 | Use [docs/ai_workflow/INDEX.md](ai_workflow/INDEX.md) + this handover as active truth; treat `.claude/SHARED_PLAN.md` as local audit history only | [docs/ai_workflow/INDEX.md](ai_workflow/INDEX.md) |
| Fatigue meter | parked by owner choice (Phase 1 + Stage 4 entry shipped; Option 1 confirmed 2026-05-13) | Treat as complete-for-now; proceed with non-fatigue work unless real logged weeks, owner labels, or explicit Phase 2 override arrive. No Phase 2 / `utils/fatigue.py` edits while parked. | [docs/fatigue_meter/PLANNING.md](fatigue_meter/PLANNING.md), [docs/fatigue_meter/STAGE4_PARKED_HANDOFF.md](fatigue_meter/STAGE4_PARKED_HANDOFF.md) |
| workout.cool integration | §3 + §5 shipped on `main`; §4 checkpoint 1 (schema+validator+apply) + checkpoint 2 (vendor assets) shipped on `main`; checkpoint 3 (mapping proposals + coverage report) shipped on branch `feat/workout-cool-section-4-checkpoint-3`; checkpoint 4 (mapping curation) **in-branch, uncommitted**: 113 reviewed rows (98 confirmed + 10 manual + 5 rejected); 79-test §4 pytest gate green | Commit the uncommitted checkpoint-4 slice (new `scripts/curate_free_exercise_db_mapping.py`, the curated `data/free_exercise_db_mapping.csv`, the EXECUTION_LOG follow-up entry, the MASTER_HANDOVER update, and the stale-assertion fix in `tests/test_free_exercise_db_mapping.py`); then open checkpoint 5 (route SELECT updates) + checkpoint 6 (thumbnail UI + `escapeHtml()` rollout). The pre-existing trailing-whitespace catalogue row at CSV line 909 still blocks the unscoped apply dry-run; pick fix path (a)/(b)/(c) per EXECUTION_LOG before / as part of checkpoint 5. | [docs/workout_cool_integration/PLANNING.md](workout_cool_integration/PLANNING.md), [docs/workout_cool_integration/EXECUTION_LOG.md](workout_cool_integration/EXECUTION_LOG.md) |
| Redesign post-P8 triage | #6 done; 5 issues remaining | #7 + #8 | local-only `debug/redesign_post_p8_issues_SESSION_STATE.md` |
| phase5_3i_plan | paused mid-draft | Resume from session-state file | local-only `debug/phase5_3i_plan_SESSION_STATE.md` |

## Open Decisions
- Choose fix path for the trailing-whitespace catalogue row (`'Dumbbell Shoulder Internal Rotation '`) before / as part of checkpoint 5. Options (a) DB-side TRIM in `utils/db_initializer.py`, (b) TRIM-on-read in `scripts/map_free_exercise_db.py`, or (c) `WHERE TRIM(exercise_name) = ?` in `scripts/apply_free_exercise_db_mapping.py`. Path (a) is the architectural fix per EXECUTION_LOG.

## Blockers
- None for new work. Unscoped apply dry-run is blocked by the pre-existing trailing-whitespace catalogue row above; scoped-to-confirmed validation passes cleanly so the in-branch curation state is otherwise apply-ready.

## Next Safe Step
Use [docs/ACTIVE_DEVELOPMENT.md](ACTIVE_DEVELOPMENT.md) as the execution source of truth. Checkpoint 4 (mapping curation) is done in-branch with 113 reviewed rows; the next safe step is to commit the checkpoint-4 slice (curation script + curated CSV + test-assertion fix + doc updates) and open checkpoint 5 — route SELECT updates in `routes/workout_plan.py` and `routes/workout_log.py` + `utils/workout_log.py` to surface `media_path` in the page/JSON contract, per [docs/workout_cool_integration/PLANNING.md](workout_cool_integration/PLANNING.md) §4.5. Vendor assets already exist under `static/vendor/free-exercise-db/`; no re-vendoring needed.
