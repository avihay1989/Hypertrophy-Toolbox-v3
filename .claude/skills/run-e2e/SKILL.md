---
description: Run Playwright E2E tests (Chromium). With no arguments runs the full suite; pass a spec filename to narrow.
argument-hint: [spec-file]
---

!`npx playwright test $ARGUMENTS --project=chromium --reporter=line`

Report pass/fail counts. Full suite takes ~7m; warn me before running without an argument.
